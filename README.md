# نور بازار | Noor Bazaar

> متجر إلكتروني متكامل - سوقك المضيء 🛒✨

متجر إلكتروني full-stack مع سلة مشتريات، لوحة تحكم، وإدارة منتجات كاملة.

## ✨ الميزات

- 🛍️ عرض المنتجات مع بحث وفلترة
- 🛒 سلة مشتريات تفاعلية
- 👤 نظام مستخدمين (تسجيل/دخول)
- 📦 لوحة تحكم لإدارة المنتجات
- 💳 صفحة طلب مع خيار الدفع عند الاستلام
- 📱 تصميم متجاوب
- 🌙 دعم الوضع الليلي

## 🚀 البدء

```bash
# تثبيت جميع الحزم (مرة واحدة)
npm run install:all

# تشغيل المشروع (الواجهة + السيرفر)
npm run dev
```

### ⚠️ فتح الموقع

**المتجر يفتح من هذا الرابط فقط:**

**http://localhost:5173**

- اكتب الرابط في المتصفح (Chrome, Edge, Firefox) واضغط Enter.
- لا تستخدم **localhost:3000** لعرض المتجر — هذا منفذ السيرفر فقط.

**على Windows:** يمكنك تشغيل الملف `run.bat` ثم فتح http://localhost:5173

## 🛠️ التقنيات

- **Frontend:** React + Vite + Tailwind CSS
- **Backend:** Node.js + Express
- **قاعدة البيانات:** JSON (قابل للترقية إلى MongoDB/PostgreSQL)

## 🌱 تعبئة قاعدة البيانات (Seed)

إذا كنت تستخدم PostgreSQL (مثلاً Railway):

```bash
cd server
npm run seed
```

هذا يضيف **75 منتج** (25 إلكترونيات، 25 ملابس، 25 عطور) مع أسماء ووصف عربي وأسعار بالدولار وصور من Unsplash. المنتجات القديمة تُحذف قبل الإضافة.

## 📁 هيكل المشروع

```
noor-bazaar/
├── client/          # واجهة المستخدم
├── server/          # السيرفر والـ API
│   ├── schema.sql   # جداول PostgreSQL
│   └── seed.js      # سكربت تعبئة المنتجات
└── README.md
```
